export interface DepartmentOption { id: string; name: string; code: string; }
export type EmployeeListStatus = 'Active' | 'Inactive' | 'OnLeave' | 'Suspended' | 'Terminated';
export type EmployeeOperationalRole = 'COLLECTOR' | 'ADMIN' | 'SUPERVISOR';
export interface EmployeeOrganizationAssignment { id: string; code: string; nameArabic: string; nameEnglish: string; organizationType: string; }
export interface EmployeeListItem { id: string; employeeNumber: string; fullName: string; departmentId: string; departmentName: string; departmentCode: string; positionId?: string | null; positionName?: string | null; operationalRole?: EmployeeOperationalRole | null; isActive: boolean; status?: EmployeeListStatus; isArchived?: boolean; organizations?: EmployeeOrganizationAssignment[]; }
export interface EmployeeDetails extends EmployeeListItem { nationalId: string | null; mobileNumber: string | null; workStartDate: string | null; fingerprintEnrollmentDate: string | null; dateOfBirth: string | null; address: string | null; workEndDate: string | null; createdAt: string; updatedAt: string | null; archivedAt: string | null; archiveReason: string | null; }
export interface PagedEmployees { items: EmployeeListItem[]; totalCount: number; page: number; pageSize: number; totalPages: number; }
export interface SaveEmployeeRequest { employeeNumber: string; fullName: string; nationalId: string; mobileNumber: string | null; departmentId: string; positionId: string; operationalRole: EmployeeOperationalRole | ''; workStartDate: string; fingerprintEnrollmentDate: string | null; dateOfBirth: string | null; address: string | null; workEndDate: string | null; isActive: boolean; }
