import { CheckCircle2, Download, Eye, FileCheck2, FileText, FolderOpen, RefreshCw, Search, Trash2, Upload, XCircle } from 'lucide-react';
import { useCallback, useEffect, useState } from 'react';
import { useSearchParams } from 'react-router-dom';
import { Button } from '../../components/common/Button';
import { Card } from '../../components/common/Card';
import { EmptyState } from '../../components/common/EmptyState';
import { ErrorState } from '../../components/common/ErrorState';
import { LoadingSpinner } from '../../components/common/LoadingSpinner';
import { Modal } from '../../components/common/Modal';
import { PageHeader } from '../../components/common/PageHeader';
import { Pagination } from '../../components/common/Pagination';
import { StatusBadge } from '../../components/common/StatusBadge';
import { useToast } from '../../components/common/Toast';
import { FileInput } from '../../components/forms/FileInput';
import { SelectInput } from '../../components/forms/SelectInput';
import { useLocalization } from '../../context/LocalizationContext';
import { HrEmployeeDocumentBulkUploadModal } from '../../features/hr/components/HrEmployeeDocumentBulkUploadModal';
import { hrEmployeeDocumentService } from '../../features/hr/services/hrEmployeeDocumentService';
import { hrMasterDataService } from '../../features/hr/services/hrMasterDataService';
import type { EmployeePersonnelFile, PagedEmployeePersonnelFiles, PersonnelDocumentChecklistItem, PersonnelFileQuery, PersonnelFileSummary, RequiredDocumentCode } from '../../features/hr/types/document';
import type { MasterDataLookup } from '../../features/hr/types/masterData';
import { getApiErrorMessage } from '../../services/apiClient';

const emptyPage: PagedEmployeePersonnelFiles = { items: [], page: 1, pageSize: 20, totalCount: 0, totalPages: 0 };
const emptySummary: PersonnelFileSummary = { totalEmployees: 0, completeFiles: 0, incompleteFiles: 0, missingDocuments: 0 };
const copy = {
  en: { title: 'Employee Documents', subtitle: 'Personnel-file completion and required-document checklist', total: 'Total Employees', completeFiles: 'Complete Files', incompleteFiles: 'Incomplete Files', missingDocuments: 'Missing Documents', search: 'Search employee name, code, or national ID', allDepartments: 'All departments', allPositions: 'All positions', allGenders: 'All genders', male: 'Male', female: 'Female', allStatuses: 'All completion statuses', complete: 'Complete', incomplete: 'Incomplete', allMissing: 'All missing-document types', employee: 'Employee', department: 'Department', position: 'Position', gender: 'Gender', completed: 'Documents Completed', missing: 'Documents Missing', completion: 'Completion', status: 'Status', actions: 'Actions', manage: 'View File / Manage Documents', archived: 'Archived', fileTitle: 'Employee Personnel File', nationalId: 'National ID', uploaded: 'Uploaded', notUploaded: 'Missing', upload: 'Upload Document', view: 'View', download: 'Download', replace: 'Replace', remove: 'Delete Document', uploadedAt: 'Uploaded', uploadedBy: 'Uploaded by', uploadFile: 'Upload {document}', replaceFile: 'Replace {document}', drag: 'Drag the file here or choose a file', formats: 'PDF / JPG / JPEG / PNG · maximum 10 MB', choose: 'Choose File', cancel: 'Cancel', submitUpload: 'Upload Document', deleteTitle: 'Delete Document', deleteConfirm: 'Are you sure you want to delete this document?', deleted: 'Document deleted.', saved: 'Document uploaded.', replaced: 'Document replaced.', loadError: 'Unable to load employee personnel files.', fileError: 'Unable to load this employee personnel file.', uploadError: 'Unable to upload the document.', deleteError: 'Unable to delete the document.', previewError: 'Unable to preview the document.', downloadError: 'Unable to download the document.', invalidFile: 'Choose a PDF, JPG, JPEG, or PNG file no larger than 10 MB.', noEmployees: 'No employees match these filters.', refresh: 'Refresh', bulkUpload: 'Bulk Upload Employee Documents', fileComplete: 'Complete File', fileIncomplete: 'Incomplete File', unknown: 'Not specified', birth: 'Birth Certificate', graduation: 'Graduation Certificate', idCopy: 'National ID Copy', military: 'Military Exemption / Status', criminal: 'Criminal Record', appointment: 'Employment / Appointment Paper', labor: 'Labor Office Registration (Kaab El Amal)' },
  ar: { title: 'مستندات الموظفين', subtitle: 'متابعة اكتمال ملف الموظف وقائمة المستندات المطلوبة', total: 'إجمالي الموظفين', completeFiles: 'ملفات مكتملة', incompleteFiles: 'ملفات غير مكتملة', missingDocuments: 'مستندات ناقصة', search: 'ابحث باسم الموظف أو الكود أو الرقم القومي', allDepartments: 'كل الأقسام', allPositions: 'كل المسميات الوظيفية', allGenders: 'كل الأنواع', male: 'ذكر', female: 'أنثى', allStatuses: 'كل حالات الاكتمال', complete: 'مكتمل', incomplete: 'غير مكتمل', allMissing: 'كل المستندات الناقصة', employee: 'الموظف', department: 'القسم', position: 'المسمى الوظيفي', gender: 'النوع', completed: 'المستندات المكتملة', missing: 'المستندات الناقصة', completion: 'نسبة الاكتمال', status: 'الحالة', actions: 'الإجراءات', manage: 'عرض الملف / إدارة المستندات', archived: 'مؤرشف', fileTitle: 'ملف مستندات الموظف', nationalId: 'الرقم القومي', uploaded: 'مرفوع', notUploaded: 'غير مرفوع', upload: 'رفع المستند', view: 'عرض', download: 'تحميل', replace: 'استبدال', remove: 'حذف المستند', uploadedAt: 'تاريخ الرفع', uploadedBy: 'رفع بواسطة', uploadFile: 'رفع {document}', replaceFile: 'استبدال {document}', drag: 'اسحب الملف هنا أو اختر ملفًا', formats: 'PDF / JPG / JPEG / PNG · بحد أقصى 10 ميجابايت', choose: 'اختيار ملف', cancel: 'إلغاء', submitUpload: 'رفع المستند', deleteTitle: 'حذف المستند', deleteConfirm: 'هل أنت متأكد من حذف هذا المستند؟', deleted: 'تم حذف المستند.', saved: 'تم رفع المستند.', replaced: 'تم استبدال المستند.', loadError: 'تعذر تحميل ملفات مستندات الموظفين.', fileError: 'تعذر تحميل ملف مستندات الموظف.', uploadError: 'تعذر رفع المستند.', deleteError: 'تعذر حذف المستند.', previewError: 'تعذر عرض المستند.', downloadError: 'تعذر تحميل المستند.', invalidFile: 'اختر ملف PDF أو JPG أو JPEG أو PNG لا يتجاوز 10 ميجابايت.', noEmployees: 'لا يوجد موظفون مطابقون لعوامل التصفية.', refresh: 'تحديث', bulkUpload: 'رفع مستندات الموظفين دفعة واحدة', fileComplete: 'ملف مكتمل', fileIncomplete: 'ملف غير مكتمل', unknown: 'غير محدد', birth: 'شهادة الميلاد', graduation: 'شهادة التخرج', idCopy: 'صورة البطاقة', military: 'ورق الإعفاء / موقف التجنيد', criminal: 'الفيش الجنائي', appointment: 'ورقة التعيين', labor: 'كعب العمل' },
} as const;

type Text = typeof copy.en | typeof copy.ar;
function docName(code: RequiredDocumentCode, text: Text) { const names: Record<RequiredDocumentCode, string> = { BIRTH_CERTIFICATE: text.birth, GRADUATION_CERTIFICATE: text.graduation, NATIONAL_ID_COPY: text.idCopy, MILITARY_STATUS: text.military, CRIMINAL_RECORD: text.criminal, EMPLOYMENT_APPOINTMENT_PAPER: text.appointment, LABOR_OFFICE_REGISTRATION: text.labor }; return names[code]; }

function UploadModal({ employee, document, onClose, onSaved }: { employee: EmployeePersonnelFile; document: PersonnelDocumentChecklistItem; onClose: () => void; onSaved: () => void }) {
  const { language } = useLocalization(); const text = copy[language]; const toast = useToast();
  const [file, setFile] = useState<File | null>(null); const [saving, setSaving] = useState(false); const [error, setError] = useState(''); const name = docName(document.code, text);
  async function submit(event: React.FormEvent) { event.preventDefault(); setError(''); if (!file || file.size > 10 * 1024 * 1024 || !/\.(pdf|jpe?g|png)$/i.test(file.name)) { setError(text.invalidFile); return; } setSaving(true); try { await hrEmployeeDocumentService.uploadRequired(employee.employeeId, document.code, file); toast.success(document.isUploaded ? text.replaced : text.saved); onSaved(); } catch (requestError) { setError(getApiErrorMessage(requestError, text.uploadError)); } finally { setSaving(false); } }
  return <Modal closeOnBackdrop={!saving} onClose={onClose} open size="md" title={(document.isUploaded ? text.replaceFile : text.uploadFile).replace('{document}', name)}><form className="space-y-5" onSubmit={submit}><div className="rounded-xl bg-mis-surface p-4 text-sm"><p className="font-bold text-mis-navy">{employee.employeeName} — {employee.employeeNumber}</p><p className="mt-1 text-slate-500">{name}</p></div>{error ? <div className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</div> : null}<div className="rounded-2xl border-2 border-dashed border-mis-border bg-slate-50 p-7 text-center"><Upload className="mx-auto h-9 w-9 text-mis-primary" /><p className="mt-3 font-semibold text-mis-navy">{text.drag}</p><p className="mt-1 text-xs text-slate-500">{text.formats}</p><div className="mx-auto mt-4 max-w-sm"><FileInput accept=".pdf,.jpg,.jpeg,.png,application/pdf,image/jpeg,image/png" label={text.choose} onChange={(event) => setFile(event.target.files?.[0] ?? null)} required /></div></div><div className="flex justify-end gap-2"><Button disabled={saving} fullWidth={false} onClick={onClose} type="button" variant="outline">{text.cancel}</Button><Button fullWidth={false} isLoading={saving} type="submit">{text.submitUpload}</Button></div></form></Modal>;
}

function PersonnelFileModal({ employeeId, onClose, onChanged }: { employeeId: string; onClose: () => void; onChanged: () => void }) {
  const { language } = useLocalization(); const text = copy[language]; const locale = language === 'ar' ? 'ar-EG' : 'en-GB'; const toast = useToast();
  const [employee, setEmployee] = useState<EmployeePersonnelFile | null>(null); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [uploading, setUploading] = useState<PersonnelDocumentChecklistItem | null>(null); const [deleting, setDeleting] = useState<PersonnelDocumentChecklistItem | null>(null);
  const load = useCallback(async () => { setLoading(true); setError(''); try { setEmployee(await hrEmployeeDocumentService.getPersonnelFile(employeeId)); } catch (requestError) { setError(getApiErrorMessage(requestError, text.fileError)); } finally { setLoading(false); } }, [employeeId, text.fileError]); useEffect(() => { void load(); }, [load]);
  async function preview(document: PersonnelDocumentChecklistItem) { if (!document.documentId) return; try { const file = await hrEmployeeDocumentService.preview(document.documentId); const url = URL.createObjectURL(file.blob); window.open(url, '_blank', 'noopener,noreferrer'); window.setTimeout(() => URL.revokeObjectURL(url), 60_000); } catch (e) { toast.error(getApiErrorMessage(e, text.previewError)); } }
  async function download(document: PersonnelDocumentChecklistItem) { if (!document.documentId || !document.fileName) return; try { await hrEmployeeDocumentService.download(document.documentId, document.fileName); } catch (e) { toast.error(getApiErrorMessage(e, text.downloadError)); } }
  async function remove() { if (!deleting?.documentId) return; try { await hrEmployeeDocumentService.delete(deleting.documentId, null); toast.success(text.deleted); setDeleting(null); await load(); onChanged(); } catch (e) { toast.error(getApiErrorMessage(e, text.deleteError)); } }
  return <Modal bodyClassName="p-0" onClose={onClose} open size="xl" title={text.fileTitle}>{loading ? <div className="flex min-h-80 items-center justify-center"><LoadingSpinner /></div> : error || !employee ? <div className="p-6"><ErrorState compact message={error} onRetry={() => void load()} title={text.fileError} /></div> : <><div className="border-b border-mis-border bg-mis-surface p-5 sm:p-6"><div className="flex flex-wrap justify-between gap-4"><div><h3 className="text-xl font-bold text-mis-navy">{employee.employeeName}</h3><p className="mt-1 text-sm text-slate-500">{employee.employeeNumber} · {employee.departmentName} · {employee.positionName ?? text.unknown}</p><p className="mt-1 text-sm text-slate-500">{text.gender}: {employee.gender ?? text.unknown}{employee.nationalId ? ` · ${text.nationalId}: ${employee.nationalId}` : ''}</p></div><div className="text-end"><StatusBadge dot tone={employee.missingDocuments === 0 ? 'success' : 'warning'}>{employee.missingDocuments === 0 ? text.fileComplete : text.fileIncomplete}</StatusBadge><p className="mt-2 text-lg font-bold text-mis-navy">{employee.completedDocuments}/{employee.requiredDocuments} · {employee.completionPercentage}%</p></div></div></div><div className="divide-y divide-mis-border">{employee.documents.map((document) => <section className="p-5 sm:p-6" key={document.code}><div className="flex flex-col justify-between gap-4 lg:flex-row lg:items-center"><div className="flex gap-3">{document.isUploaded ? <CheckCircle2 className="h-6 w-6 text-emerald-600" /> : <XCircle className="h-6 w-6 text-amber-500" />}<div><h4 className="font-bold text-mis-navy">{docName(document.code, text)}</h4><div className="mt-2"><StatusBadge dot tone={document.isUploaded ? 'success' : 'warning'}>{document.isUploaded ? text.uploaded : text.notUploaded}</StatusBadge></div>{document.uploadedAt ? <p className="mt-2 text-xs text-slate-500">{text.uploadedAt}: {new Intl.DateTimeFormat(locale, { dateStyle: 'medium' }).format(new Date(document.uploadedAt))}{document.uploadedBy ? ` · ${text.uploadedBy}: ${document.uploadedBy}` : ''}</p> : null}</div></div><div className="flex flex-wrap gap-2">{document.isUploaded ? <><Button fullWidth={false} onClick={() => void preview(document)} size="sm" variant="outline"><Eye className="h-4 w-4" />{text.view}</Button><Button fullWidth={false} onClick={() => void download(document)} size="sm" variant="outline"><Download className="h-4 w-4" />{text.download}</Button><Button fullWidth={false} onClick={() => setUploading(document)} size="sm" variant="outline"><RefreshCw className="h-4 w-4" />{text.replace}</Button><Button fullWidth={false} onClick={() => setDeleting(document)} size="sm" variant="danger"><Trash2 className="h-4 w-4" />{text.remove}</Button></> : <Button fullWidth={false} onClick={() => setUploading(document)} size="sm"><Upload className="h-4 w-4" />{text.upload}</Button>}</div></div></section>)}</div>{uploading ? <UploadModal document={uploading} employee={employee} onClose={() => setUploading(null)} onSaved={() => { setUploading(null); void load(); onChanged(); }} /> : null}{deleting ? <Modal dialogRole="alertdialog" footer={<><Button fullWidth={false} onClick={() => setDeleting(null)} variant="outline">{text.cancel}</Button><Button fullWidth={false} onClick={() => void remove()} variant="danger">{text.remove}</Button></>} onClose={() => setDeleting(null)} open size="sm" title={text.deleteTitle}><p>{text.deleteConfirm}</p></Modal> : null}</>}</Modal>;
}

export function HrEmployeeDocumentsPage() {
  const { language } = useLocalization(); const text = copy[language]; const [searchParams] = useSearchParams();
  const [query, setQuery] = useState<PersonnelFileQuery>({ page: 1, pageSize: 20, search: '', employeeId: searchParams.get('employeeId') ?? '', departmentId: '', positionId: '', gender: '', completionStatus: 'All', missingDocumentCode: '' }); const [search, setSearch] = useState(''); const [data, setData] = useState(emptyPage); const [summary, setSummary] = useState(emptySummary); const [departments, setDepartments] = useState<MasterDataLookup[]>([]); const [positions, setPositions] = useState<MasterDataLookup[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [selected, setSelected] = useState<string | null>(query.employeeId || null); const [bulkOpen, setBulkOpen] = useState(false);
  useEffect(() => { const timer = window.setTimeout(() => setQuery((value) => ({ ...value, page: 1, search: search.trim() })), 300); return () => window.clearTimeout(timer); }, [search]);
  const load = useCallback(async () => { setLoading(true); setError(''); try { const [page, totals] = await Promise.all([hrEmployeeDocumentService.getPersonnelFiles(query), hrEmployeeDocumentService.getPersonnelFileSummary()]); setData(page); setSummary(totals); } catch (e) { setError(getApiErrorMessage(e, text.loadError)); } finally { setLoading(false); } }, [query, text.loadError]); useEffect(() => { void load(); }, [load]);
  useEffect(() => { void Promise.all([hrMasterDataService.getLookup('departments', true), hrMasterDataService.getLookup('positions', true)]).then(([d, p]) => { setDepartments(d); setPositions(p); }); }, []);
  const filter = (value: Partial<PersonnelFileQuery>) => setQuery((current) => ({ ...current, ...value, page: 1 }));
  const cards = [{ label: text.total, value: summary.totalEmployees, icon: <FolderOpen /> }, { label: text.completeFiles, value: summary.completeFiles, icon: <FileCheck2 /> }, { label: text.incompleteFiles, value: summary.incompleteFiles, icon: <FileText /> }, { label: text.missingDocuments, value: summary.missingDocuments, icon: <XCircle /> }];
  return <div className="space-y-6"><PageHeader actions={<div className="flex flex-wrap gap-2"><Button fullWidth={false} onClick={() => setBulkOpen(true)}><Upload className="h-4 w-4" />{text.bulkUpload}</Button><Button fullWidth={false} onClick={() => void load()} variant="outline"><RefreshCw className="h-4 w-4" />{text.refresh}</Button></div>} description={text.subtitle} title={text.title} /><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map((card) => <Card className="p-5" key={card.label}><div className="flex items-center justify-between"><div><p className="text-sm font-semibold text-slate-500">{card.label}</p><p className="mt-2 text-3xl font-bold text-mis-navy">{card.value}</p></div><div className="rounded-xl bg-mis-pale p-3 text-mis-primary">{card.icon}</div></div></Card>)}</div><Card className="p-4"><div className="grid gap-3 md:grid-cols-2 lg:grid-cols-3 min-[1200px]:grid-cols-[minmax(220px,2fr)_minmax(0,1fr)_minmax(0,1fr)_minmax(0,0.8fr)_minmax(0,1fr)_minmax(0,1fr)]"><label className="relative"><Search className="absolute start-3 top-3 h-5 w-5 text-slate-400" /><input className="h-11 w-full rounded-xl border border-mis-border pe-3 ps-10 text-sm outline-none" onChange={(e) => setSearch(e.target.value)} placeholder={text.search} value={search} /></label><SelectInput label={text.department} onChange={(e) => filter({ departmentId: e.target.value })} value={query.departmentId}><option value="">{text.allDepartments}</option>{departments.map((item) => <option key={item.id} value={item.id}>{language === 'ar' && item.nameArabic ? item.nameArabic : item.nameEnglish}</option>)}</SelectInput><SelectInput label={text.position} onChange={(e) => filter({ positionId: e.target.value })} value={query.positionId}><option value="">{text.allPositions}</option>{positions.map((item) => <option key={item.id} value={item.id}>{language === 'ar' && item.nameArabic ? item.nameArabic : item.nameEnglish}</option>)}</SelectInput><SelectInput label={text.gender} onChange={(e) => filter({ gender: e.target.value })} value={query.gender}><option value="">{text.allGenders}</option><option value="Male">{text.male}</option><option value="Female">{text.female}</option></SelectInput><SelectInput label={text.status} onChange={(e) => filter({ completionStatus: e.target.value as PersonnelFileQuery['completionStatus'] })} value={query.completionStatus}><option value="All">{text.allStatuses}</option><option value="Complete">{text.complete}</option><option value="Incomplete">{text.incomplete}</option></SelectInput><SelectInput label={text.missingDocuments} onChange={(e) => filter({ missingDocumentCode: e.target.value as PersonnelFileQuery['missingDocumentCode'] })} value={query.missingDocumentCode}><option value="">{text.allMissing}</option><option value="BIRTH_CERTIFICATE">{text.birth}</option><option value="GRADUATION_CERTIFICATE">{text.graduation}</option><option value="NATIONAL_ID_COPY">{text.idCopy}</option><option value="MILITARY_STATUS">{text.military}</option><option value="CRIMINAL_RECORD">{text.criminal}</option><option value="EMPLOYMENT_APPOINTMENT_PAPER">{text.appointment}</option><option value="LABOR_OFFICE_REGISTRATION">{text.labor}</option></SelectInput></div></Card><Card className="overflow-hidden">{loading ? <div className="flex min-h-72 items-center justify-center"><LoadingSpinner /></div> : error ? <ErrorState message={error} onRetry={() => void load()} title={text.loadError} /> : data.items.length === 0 ? <EmptyState description={text.noEmployees} icon={<FileText className="h-6 w-6" />} title={text.noEmployees} /> : <><div className="overflow-x-auto"><table className="min-w-full text-sm"><thead className="bg-mis-surface text-xs uppercase text-slate-500"><tr>{[text.employee, text.department, text.position, text.gender, text.completed, text.missing, text.completion, text.status, text.actions].map((label) => <th className="px-4 py-3 text-start" key={label}>{label}</th>)}</tr></thead><tbody className="divide-y divide-mis-border">{data.items.map((employee) => <tr className="hover:bg-slate-50" key={employee.employeeId}><td className="px-4 py-4"><p className="font-bold text-mis-navy">{employee.employeeName}</p><p className="text-xs text-slate-500">{employee.employeeNumber}{employee.isArchived ? ` · ${text.archived}` : ''}</p></td><td className="px-4 py-4">{employee.departmentName}</td><td className="px-4 py-4">{employee.positionName ?? text.unknown}</td><td className="px-4 py-4">{employee.gender === 'Female' ? text.female : employee.gender === 'Male' ? text.male : text.unknown}</td><td className="px-4 py-4 font-semibold">{employee.completedDocuments} / {employee.requiredDocuments}</td><td className="px-4 py-4">{employee.missingDocuments}</td><td className="min-w-40 px-4 py-4"><div className="h-2 rounded-full bg-slate-100"><div className="h-2 rounded-full bg-mis-primary" style={{ width: `${employee.completionPercentage}%` }} /></div><p className="mt-1 text-xs font-semibold">{employee.completionPercentage}%</p></td><td className="px-4 py-4"><StatusBadge dot tone={employee.missingDocuments === 0 ? 'success' : 'warning'}>{employee.missingDocuments === 0 ? text.complete : text.incomplete}</StatusBadge></td><td className="px-4 py-4"><Button fullWidth={false} onClick={() => setSelected(employee.employeeId)} size="sm" variant="outline"><Eye className="h-4 w-4" />{text.manage}</Button></td></tr>)}</tbody></table></div><Pagination labels={{ nextPage: '›', pageOf: (page, total) => `${page} / ${total}`, previousPage: '‹', showing: (from, to, total) => `${from}-${to} / ${total}` }} onPageChange={(page) => setQuery((value) => ({ ...value, page }))} page={data.page} pageSize={data.pageSize} totalCount={data.totalCount} totalPages={data.totalPages} /></>}</Card>{selected ? <PersonnelFileModal employeeId={selected} onChanged={() => void load()} onClose={() => setSelected(null)} /> : null}{bulkOpen ? <HrEmployeeDocumentBulkUploadModal onClose={() => setBulkOpen(false)} onCompleted={() => { void load(); }} /> : null}</div>;
}
